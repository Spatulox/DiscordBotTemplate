/**
 * One entry per bot. The value MUST match the folder name under src/,
 * it is used to build the path to the handler json files.
 */
export enum BotType {
    BOT_ONE = "bot_one",
    BOT_TWO = "bot_two",
}
